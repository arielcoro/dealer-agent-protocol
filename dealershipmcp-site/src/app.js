const form=document.querySelector("#audit-form");
if(form){const status=document.querySelector("#form-status");form.addEventListener("submit",async event=>{event.preventDefault();const button=form.querySelector("button[type=submit]");button.disabled=true;status.hidden=true;try{const response=await fetch(form.action,{method:"POST",headers:{Accept:"application/json"},body:new FormData(form)});const result=await response.json();if(!response.ok)throw new Error(result.error||"request_failed");form.reset();status.textContent="Your audit request is in. We’ll review the dealership and follow up at the work email you provided.";status.hidden=false}catch(error){status.textContent="We couldn’t save the request. Please try again, or email ariel@dealergrowthhackers.com.";status.hidden=false}finally{button.disabled=false}})}
